# Leave MODEL_PATH empty until you have an authorised custom PPE model.
MODEL_PATH=
ALLOWED_ORIGINS=http://localhost:5173
DATABASE_URL=sqlite:///./app/data/safeaudit.db

# Local dashboard-only sample events. Keep true only while learning the MVP flow.
DEMO_MODE=true
